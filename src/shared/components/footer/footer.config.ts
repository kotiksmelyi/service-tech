import { OverrideToken } from 'antd/es/theme/interface';
import { colors } from '../../ui';

export const footerConfig: OverrideToken['Layout'] = {
    footerBg: colors.BLACK,
    footerPadding: 0,
    colorText: colors.GREY
};