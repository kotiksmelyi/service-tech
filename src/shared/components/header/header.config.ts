import { OverrideToken } from 'antd/es/theme/interface';
import { colors } from '../../ui';

export const headerConfig: OverrideToken['Layout'] = {
    headerBg: colors.BLACK,
    headerPadding: 0,
    headerColor: colors.WHITE,
    bodyBg: colors.BLACK,
};